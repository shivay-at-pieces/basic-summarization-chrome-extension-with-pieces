# basic-summarization-chrome-extension-with-pieces

## Running locally 
- First build the project using `npm run build`
- In Chrome extensions, click on load unpacked
- Add the dist folder
- Run the extension
